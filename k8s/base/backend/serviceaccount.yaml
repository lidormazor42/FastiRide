apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend
  namespace: fastiride
  annotations:
    # Replaced by overlay patch with the actual IRSA ARN from Terraform output
    eks.amazonaws.com/role-arn: REPLACE_WITH_BACKEND_IRSA_ROLE_ARN
