backend:
  replicas: 1
  irsa:
    roleArn: ""   # paste: terraform output backend_irsa_role_arn

frontend:
  replicas: 1

ingress:
  host: dev.fastiride.example.com

config:
  googleRedirectUri: https://dev.fastiride.example.com/api/auth/google/callback

hpa:
  enabled: false
